# Copy to .env and fill in to use real (test-mode) Stripe billing.
# Without these, the server runs in mock-checkout mode — the whole
# submission -> verification -> "payment" -> featured pipeline still works,
# it just skips talking to Stripe at all.

STRIPE_SECRET_KEY=
STRIPE_PRICE_ID=
STRIPE_WEBHOOK_SECRET=
PORT=8790
